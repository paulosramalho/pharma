# --- Segurança avançada ---
# Rate limit
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120
RATE_LIMIT_KEYBY=ip
RATE_LIMIT_SKIP_PATHS=/health/live,/health/ready

# IP control (deixe vazio para não bloquear)
IP_ALLOWLIST=
IP_DENYLIST=
IP_CONTROL_SKIP_PATHS=/health/live,/health/ready

# 2FA
TWOFA_ISSUER=Pharma
TWOFA_DIGITS=6
TWOFA_STEP=30

# Encryption key for 2FA secret (AES-256-GCM)
# 64 hex chars (32 bytes)
APP_ENC_KEY=
